<?php

// git checkout ... ===> switch branch

// git branch -l ==> list

// git branch (name branch) ==> create

// git commit -a -m "" =>>>  a میاد کار Add رو انجام میده

//git checkout -b (name branch) ==> اول میسازه بعدش خود سوسیچ میکنه

//git merge branch => برنچ مقصد نوشته میشه یعنی محتویات ان برنچ داخل برنج مبدا

//git branch -D (name) ==> همان برانچ فعلی نمیشه باید برنچی که داخلش میستید نویسید ت حذف بسشه
